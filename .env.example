# Optional. Without it the app works fully; only the advisory AI features
# (effort estimation, task decomposition, conversational replan) go dark.
# The solver never depends on this key.
GEMINI_API_KEY=
